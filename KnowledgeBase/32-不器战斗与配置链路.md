# 模块 32：不器战斗与配置链路

> Catalog ID: `BUQI-01`  
> 状态：`verified`  
> 最后核验：`2026-08-05`  
> 适用模式：GameHot / Editor / .NET Headless

## 模块定位

本模块覆盖《不器》当前稳定的 Battle/Headless/Charge/Step 3 配置链路。它回答“战斗内核如何保持纯 C# 确定性、批准哈希如何阻断漂移、Luban 最小内容如何接入战斗定义、Unity 局部门禁证明到哪里为止”。它不负责正式玩家 UI、完整经济、全 18 法门批量内容、服务端对战或全仓五项运行验收。

## 源码边界

| 类型 | 仓库相对路径 | 说明 |
| --- | --- | --- |
| 纯战斗内核 | `Unity/Assets/Scripts/Game/Hot/Code/Buqi/Battle` | 模型、规则、契约检查、测试向量和 `BuqiBattleSimulator` |
| 配置适配层 | `Unity/Assets/Scripts/Game/Hot/Code/Buqi/Config` | Luban 行模型、provider、validator 和生成表反射 adapter |
| 沙盒与菜单 | `Unity/Assets/Scripts/Game/Hot/Code/Buqi/Debug/BuqiBattleSandbox.cs`、`Unity/Assets/Scripts/Game/Hot/Code/Editor/Buqi/BuqiBattleSandboxWindow.cs` | Editor-only 调试入口，不进入正式玩家流程 |
| Headless | `Share/Buqi.Simulation.Headless` | .NET 8 验证器，直接链接同一份 `Battle/**/*.cs` |
| Unity 测试 | `Unity/Assets/Tests/GameHot/Buqi/EditMode` | Battle、Sandbox、Config 的 EditMode 局部门禁 |
| 配置源 | `Design/Excel/GameHot/Datas/Buqi/*.xlsx`、`__tables__.xlsx`、`__beans__.xlsx`、`__enums__.xlsx` | 9 法门、3 淬炼、6 道影与 schema 注册 |
| 生成输出 | `Unity/Assets/Scripts/Game/Hot/Code/Generate/Luban/DTBuqi*.cs`、`DRBuqi*.cs`、`Buqi*.cs` | Luban 自动生成，禁止手改 |
| 导出数据 | `Unity/Assets/Res/Hot/Luban/dtbuqi*.bytes`、`Unity/Assets/Res/Editor/Hot/Luban/dtbuqi*.json` | GameHot bytes 与 Editor JSON |
| 运行证据 | `production/session-state/active.md` | 主工作区 Step 1/2/Charge/Step 3 运行记录 |

## 依赖关系

- `Battle` 目录只使用 C# 标准库和仓库内自身类型；不得引用 Unity、UGF、ET、资源、场景、网络、时间或随机 API。
- `Share/Buqi.Simulation.Headless` 以 `Compile Include="..\..\Unity\Assets\Scripts\Game\Hot\Code\Buqi\Battle\**\*.cs"` 链接同一份源码，定义 `BUQI_HEADLESS`，目标 `net8.0`、C# 9，并把 warning 当错误。
- Unity EditMode 测试程序集 `Game.Hot.Buqi.Tests` 只在 Editor 与 `UNITY_GAMEHOT` 下编译，引用 `Game.Hot.Code`、TestRunner 和 NUnit，`noEngineReferences=true`。
- Luban 生成表属于 GameHot 热更 Tables；战斗内核不直接依赖 `DTBuqi*`/`DRBuqi*`，由 `BuqiGeneratedConfigAdapter` 和 `BuqiDefinitionProvider` 转成 `IItemDefinitionProvider`。

## 入口与调用链

```text
BattleRequest + IItemDefinitionProvider
  -> BuqiBattleSimulator.Simulate
  -> ValidateRequest / BuqiBoardValidator
  -> tick loop: Declare -> Resolve/Chain -> Aggregate -> PostTick
  -> stable BattleResult + BattleLogHash
```

```text
GameHot TablesComponent.LoadAllAsync
  -> generated TablesComponent.LoadAsync(loader)
  -> DTBuqiGlobal / DTBuqiItem / DTBuqiRefinement / DTBuqiEcho
  -> generated PostResolveRef
  -> TablesComponent.BuqiConfig.cs
  -> BuqiGeneratedConfigAdapter.TryReadFromTables
  -> BuqiConfigValidator.Validate
  -> BuqiDefinitionProvider
```

Headless `verify` 和 Unity `ApprovedHashes_MatchCurrentContractVectors` 都读取同一份 `Unity/Assets/Tests/GameHot/Buqi/EditMode/Vectors/approved-buqi-hashes.json`。`verify`/`all` 只读批准基线；只有显式 `update-hashes` 会写入，且写入前仍先执行 `BuqiContractChecks.RunAll()`。

## 核心类型与 API

| 类型/API | 职责 | 生命周期/线程约束 |
| --- | --- | --- |
| `BuqiBattleSimulator` | 战斗主循环、稳定排序、日志哈希和结算 | 纯函数式入口；非法请求返回 `InvalidBuild` |
| `BuqiContractChecks.RunAll()` | 独立于 approved hash 的行为契约断言 | Hash 更新前必须先通过 |
| `BuqiTestSuite.CreateVectors()` | 15 个稳定向量 | 向量 ID 是批准哈希协议的一部分 |
| `BuqiDefinitionProvider` | 将配置行深拷贝为战斗定义 | 返回副本，避免调用方改写缓存 |
| `BuqiConfigValidator` | 校验 Step 3 最小配置链 | 9/3/6 计数、ID 范围、效果字段、引用和棋盘 |
| `BuqiGeneratedConfigAdapter` | 反射读取生成表 | 避免 `Battle` 直接依赖 Luban 生成类型 |
| `BuqiBattleSandbox` | 纸面认知走查用沙盒模型 | Editor-only，不创建正式 UI/prefab |

## 数据与生命周期

- 战斗契约版本为 `RuleVersion = 0.4.1`、`SimulationVersion = battle-core-0.4.1`；1 tick = 100 ms，正常 450 tick，600 tick hard cap，单 tick 全场 64 事件、单实例 4 事件上限。
- Charge 0.4.1 在 Declare 阶段完成读取和可选消费：效果已有合法目标后才读取 `ChargeReadLimit`，`ChargeConsume=true` 时立即扣除并记录负数 `ChargeConsumed` Declare-phase 事件；Resolve 只使用 `DeclaredCharge` 快照。
- Declare 队列按 chain depth、来源锚点、来源实例 ID、原始插入顺序排序。同来源多声明因此保持配置/声明顺序，先声明的 Charge 可供后声明读取；A-03 复写复用原声明快照，不二次消费。
- 双方同 tick 的 Declare/Resolve/Chain 全部完成后才 Aggregate，再统一 PostTick 判断胜负；同 tick 双方归零会得到平局，而不是由处理顺序决定胜者。
- Step 3 Excel 源当前为 9 法门：`W8-003`、`W8-005`、`W8-006`、`W8-007`、`W8-008`、`W8-012`、`W8-013`、`W8-014`、`W8-015`；3 淬炼：A-01、A-03、A-04；6 道影：`echo-fast-lesson`、`echo-fast-early`、`echo-buffer-lesson`、`echo-buffer-early`、`echo-chain-lesson`、`echo-chain-early`；全局内容版本为 `buqi-step3-cv1`。
- `TablesComponent.BuqiConfig.cs` 只在生成表存在时生效；读取失败或业务校验失败直接抛异常，成功后缓存 `BuqiConfig` 与 `BuqiItemDefinitions`。

## 开发扩展步骤

1. 修改战斗语义时先更新 `Battle` 代码和 `BuqiContractChecks`，确认行为断言能描述变化，再由人工审阅后显式更新 approved hashes。
2. 新增 Step 3 范围内配置时先改 `Design/Excel/GameHot/Datas/Buqi` 与 schema 表，再运行 Luban Check/Export，检查 Hot generated C#、Hot bytes 和 Editor JSON 同步变化。
3. 新增效果字段必须在 `BuqiConfigModels`、`BuqiGeneratedConfigAdapter`、`BuqiConfigValidator` 与战斗 `BuqiEffectSpec` 的映射中成对落地。
4. 扩充道影要用 `BuqiBoardValidator` 证明 8 格布局、引用、淬炼 ID 和内容版本合法。
5. P-1 通过前不得进入正式玩家 UI、完整经济内容或完整 18 法门批量录入；现有 `Battle Sandbox` 只用于“预测 -> 归因 -> 针对性重构”的认知走查。

## 约束与常见错误

- 不要让 `Battle` 反向引用 Luban、Unity、UGF、ET 或资源系统；这会让 headless 编译防线失效。
- 不要把 `update-hashes` 当作普通修复命令。approved hash 只能在契约断言通过、差异经人工确认后刷新。
- `ChargeConsumed` 是 Declare-phase 负数资源事件，不是 Aggregate 日志；把消费推迟到 Resolve/Aggregate 会破坏同 tick 顺序。
- A-03 复写不能重新读取或重复消费 Charge；它必须复用原声明的 `DeclaredCharge`。
- `Check` 只验证 Luban，不生成实际代码/bytes/json；刷新配置必须执行正式导出并检查生成差异。
- 本模块记录的 Unity 14/14、菜单与 headless 证据是 Buqi 局部门禁，不改变 `runtime-acceptance.json` 五项通用验收的 `not_run` 真实性。

## 验证方法

建议的局部门禁：

1. `dotnet run --project Share/Buqi.Simulation.Headless/Buqi.Simulation.Headless.csproj -- verify`，确认行为契约和 15 个 approved hashes 通过。
2. 需要压力样本时运行 headless `all [count]` 或 `stress [count]`；stress 不读取 approved hash，也不得解释为平衡数据。
3. 在 Unity Editor、`UNITY_GAMEHOT` 下运行 `Game.Hot.Buqi.Tests`，覆盖 Battle、Sandbox 和 Config。
4. 通过 Unity 菜单 `Game/Buqi/Battle Sandbox` 打开沙盒，确认菜单入口可执行。
5. 运行 Luban Check/Export 后核对 `dtbuqi*.json` 计数和 generated/bytes diff。

本轮知识同步没有重新执行 Unity、headless、Luban 或菜单；复核的是 `1027f869` 已提交源码和 `production/session-state/active.md` 中的主工作区证据：Step 3 Unity EditMode `Game.Hot.Buqi.Tests` 为 `total=14`、`passed=14`、`failed=0`、`success=true`，菜单 `Game/Buqi/Battle Sandbox` 返回 `executed=true`，Luban Check/Export、headless `verify`、配置适配静态编译和生成 JSON 数量检查均已记录为通过。五项通用运行验收仍保持 `not_run`。

## 源码证据

- `Unity/Assets/Scripts/Game/Hot/Code/Buqi/Battle/Simulation/BuqiBattleSimulator.cs`：纯 C# 主循环、Charge 0.4.1 Declare 消费、同 tick Aggregate 和稳定排序。
- `Unity/Assets/Scripts/Game/Hot/Code/Buqi/Battle/Definition/BuqiContractChecks.cs`、`BuqiTestSuite.cs`：行为断言、15 个 approved hash 向量和 stress 样本生成。
- `Share/Buqi.Simulation.Headless/Buqi.Simulation.Headless.csproj`、`Program.cs`：headless 编译边界、验证模式和批准哈希读写规则。
- `Unity/Assets/Tests/GameHot/Buqi/EditMode/BuqiBattleTests.cs`、`BuqiBattleSandboxTests.cs`、`BuqiConfigTests.cs`、`Vectors/approved-buqi-hashes.json`：Unity 局部测试、沙盒回归、配置校验和当前批准基线。
- `Design/Excel/GameHot/Datas/Buqi/*.xlsx`、`Design/Excel/GameHot/Datas/__tables__.xlsx`、`__beans__.xlsx`、`__enums__.xlsx`：Step 3 源数据和 schema 注册。
- `Unity/Assets/Scripts/Game/Hot/Code/Buqi/Config/*.cs`、`Unity/Assets/Scripts/Game/Hot/Code/Tables/TablesComponent.BuqiConfig.cs`：Luban 生成表到战斗 provider 的适配与业务校验。
- `Unity/Assets/Scripts/Game/Hot/Code/Generate/Luban/DTBuqi*.cs`、`DRBuqi*.cs`、`Buqi*.cs`、`Unity/Assets/Res/Hot/Luban/dtbuqi*.bytes`、`Unity/Assets/Res/Editor/Hot/Luban/dtbuqi*.json`：四张 Buqi 表的生成代码和导出数据。
- `Unity/Assets/Scripts/Game/Hot/Code/Buqi/Debug/BuqiBattleSandbox.cs`、`Unity/Assets/Scripts/Game/Hot/Code/Editor/Buqi/BuqiBattleSandboxWindow.cs`：P-1 前的沙盒入口和非正式 UI 边界。
- `production/session-state/active.md`：主工作区 Step 2、Charge 0.4.1 与 Step 3 的 Unity/headless/Luban/menu 运行证据和 P-1 停止线。

## 关联知识

- 上游：`ARCH-02` 模式与程序集边界。
- 上游：`UNITY-15`、`DATA-01` Luban 生产链与客户端 Tables。
- 上游：`LIB-05` LubanLib 运行时契约。
- 上游：`PACKAGE-01` Unity/.NET 依赖边界。
- 下游：P-1 认知走查与后续正式 UI/内容门禁。
