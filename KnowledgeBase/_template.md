# 模块 XX：模块名称

> Catalog ID: `AREA-NN`  
> 状态：`seed`  
> 最后核验：`YYYY-MM-DD`  
> 适用模式：GameHot / ET Client / ET Server / Shared / Editor

## 模块定位

说明模块解决什么问题、不负责什么，以及它所属的程序集或 .NET 项目。

## 源码边界

| 类型 | 仓库相对路径 | 说明 |
| --- | --- | --- |
| 入口 | `path/to/entry` | 启动或调用入口 |
| 核心 | `path/to/module` | 主要实现 |
| 配置 | `path/to/config` | 配置源 |
| 生成物 | `path/to/generated` | 禁止手改时明确标注 |

## 依赖关系

列出上游依赖、下游调用者、程序集引用和条件编译符号。

## 入口与调用链

用步骤或 Mermaid 描述从公开入口到核心行为的真实链路。

## 核心类型与 API

| 类型/API | 职责 | 生命周期/线程约束 |
| --- | --- | --- |
| `TypeName` | ... | ... |

## 数据与生命周期

说明状态所有权、初始化、更新、关闭/回收，以及持久化或网络边界。

## 开发扩展步骤

给出新增一个最小功能的准确步骤，并指向实际调用示例。

## 约束与常见错误

记录模式差异、生成目录、宏、资源命名、线程、异步和构建约束。

## 验证方法

列出可重复的编译、命令、Unity 菜单或运行时验证，并明确区分“建议步骤”和“本轮实际执行结果”。不能执行的验证要说明环境前提；不得把静态阅读写成运行通过。跨模块运行验收记录到 `runtime-acceptance.json`。

## 源码证据

- `path/to/file.cs`：支持本文哪个结论。
- `path/to/caller.cs`：真实调用位置。

## 关联知识

- 上游：`AREA-NN`
- 下游：`AREA-NN`
